[project]
name = "chaukanna-scoring"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.43.98",
    "pydantic>=2.13.5",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.8",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
